# Turso (primary - synced across devices)
TURSO_DATABASE_URL="libsql://<your-db-name>.turso.io"
TURSO_AUTH_TOKEN="your-turso-auth-token"

# Local SQLite fallback (only used with prisma:migrate for local dev)
DATABASE_URL="file:./dev.db"

# Server port
PORT=3000

# Environment
NODE_ENV=development
