name: Deploy to Staging Server
on:
  push:
    branches: [ staging ]

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check Environment
        run: |
          echo "🔍 Environment Info:"
          echo "Node version: $(node --version)"
          echo "NPM version: $(npm --version)"
          echo "PM2 installed: $(command -v pm2 &> /dev/null && echo 'yes' || echo 'no')"

      - name: Install Dependencies
        run: |
          echo "📦 Installing dependencies..."
          npm install
          cd backend && npm install && cd ..
          cd frontend && npm install && cd ..

      - name: Setup Database
        run: |
          echo "🗄️ Setting up database..."
          cd backend
          npx prisma generate
          npx prisma migrate deploy || npx prisma db push
          cd ..

      - name: Install PM2 if needed
        run: |
          if ! command -v pm2 &> /dev/null; then
            echo "📦 Installing PM2..."
            npm install -g pm2
          else
            echo "✅ PM2 already installed: $(pm2 --version)"
          fi

      - name: Restart with PM2 (Dev Mode)
        run: |
          echo "� Restarting services in DEV mode..."
          
          pm2 stop qa-tool-backend || true
          pm2 stop qa-tool-frontend || true
          pm2 delete qa-tool-backend || true
          pm2 delete qa-tool-frontend || true
          
          # Backend dev mode (hot reload)
          cd backend
          pm2 start npm --name "qa-tool-backend" --interpreter none -- run dev
          cd ..
          
          # Frontend dev mode (Vite dev server)
          cd frontend
          pm2 start npm --name "qa-tool-frontend" --interpreter none -- run dev -- --host
          cd ..
          
          pm2 save

      - name: Show Status
        run: |
          echo ""
          echo "✅ Deployment Complete (DEV MODE)!"
          echo ""
          pm2 list
          echo ""
          echo "🌐 Access:"
          echo "   Frontend: http://$(hostname -I | awk '{print $1}'):5173"
          echo "   Backend:  http://$(hostname -I | awk '{print $1}'):3000"
          echo ""
          echo "💡 Running in development mode with hot reload"
